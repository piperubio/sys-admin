# Users
## CHANGE DEFAULT USERNAME FOR YOUR USER
default_username: DEFAULT_USERNAME

# generate random passwords for default user and root user
default_password_generated: "USER_PASSWORD"
root_password_generated: "ROOT_PASSWORD"

# Docker
## CHANGE DEFAULT USERNAME FOR YOUR USER
docker_users:
  - "DEFAULT_USERNAME"